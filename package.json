{
  "name": "whatsapp-inbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply whatsapp_inbox_db",
    "db:migrate:local": "wrangler d1 migrations apply whatsapp_inbox_db --local"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
